<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>안전한 결제 - 운세 코인 충전</title>
    <script src="https://cdn.iamport.kr/v1/iamport.js"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        
        .payment-container {
            background: white;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            max-width: 500px;
            width: 100%;
            text-align: center;
        }
        
        .logo {
            font-size: 2rem;
            font-weight: bold;
            color: #667eea;
            margin-bottom: 10px;
        }
        
        .subtitle {
            color: #666;
            margin-bottom: 30px;
        }
        
        .package-info {
            background: #f8f9fa;
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 30px;
            border: 2px solid #e9ecef;
        }
        
        .package-title {
            font-size: 1.3rem;
            font-weight: bold;
            color: #333;
            margin-bottom: 15px;
        }
        
        .coin-info {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 10px;
        }
        
        .coin-amount {
            font-size: 1.1rem;
            color: #667eea;
            font-weight: bold;
        }
        
        .price {
            font-size: 1.5rem;
            font-weight: bold;
            color: #333;
            margin-top: 10px;
        }
        
        .payment-methods {
            margin-bottom: 30px;
        }
        
        .method-title {
            font-size: 1.1rem;
            font-weight: bold;
            margin-bottom: 15px;
            color: #333;
        }
        
        .method-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 10px;
        }
        
        .method-btn {
            padding: 15px;
            border: 2px solid #e9ecef;
            border-radius: 10px;
            background: white;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 0.9rem;
        }
        
        .method-btn:hover {
            border-color: #667eea;
            background: #f8f9ff;
        }
        
        .method-btn.selected {
            border-color: #667eea;
            background: #667eea;
            color: white;
        }
        
        .pay-button {
            width: 100%;
            padding: 18px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 15px;
            font-size: 1.1rem;
            font-weight: bold;
            cursor: pointer;
            transition: transform 0.2s ease;
            margin-bottom: 20px;
        }
        
        .pay-button:hover {
            transform: translateY(-2px);
        }
        
        .pay-button:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }
        
        .security-info {
            font-size: 0.85rem;
            color: #666;
            line-height: 1.4;
        }
        
        .back-link {
            color: #667eea;
            text-decoration: none;
            font-size: 0.9rem;
            margin-top: 15px;
            display: inline-block;
        }
        
        .loading {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0,0,0,0.8);
            z-index: 9999;
            align-items: center;
            justify-content: center;
        }
        
        .loading-content {
            background: white;
            padding: 30px;
            border-radius: 15px;
            text-align: center;
        }
        
        .spinner {
            width: 40px;
            height: 40px;
            border: 4px solid #f3f3f3;
            border-top: 4px solid #667eea;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin: 0 auto 15px;
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        @media (max-width: 480px) {
            .payment-container {
                padding: 25px;
            }
            
            .method-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="payment-container">
        <div class="logo">🔮 사주운세</div>
        <div class="subtitle">안전한 결제 시스템</div>
        
        <div class="package-info">
            <div class="package-title"><%= sessionData.packageName || '코인 패키지' %></div>
            <div class="coin-info">
                <span>기본 코인:</span>
                <span class="coin-amount"><%= sessionData.coins %>개</span>
            </div>
            <% if (sessionData.bonusCoins && sessionData.bonusCoins > 0) { %>
            <div class="coin-info">
                <span>보너스 코인:</span>
                <span class="coin-amount">+<%= sessionData.bonusCoins %>개</span>
            </div>
            <% } %>
            <div class="price">₩<%= sessionData.amount.toLocaleString() %></div>
        </div>
        
        <div class="payment-methods">
            <div class="method-title">결제 방법 선택</div>
            <div class="method-grid">
                <div class="method-btn" data-method="card">신용카드</div>
                <div class="method-btn" data-method="trans">실시간 계좌이체</div>
                <div class="method-btn" data-method="vbank">가상계좌</div>
                <div class="method-btn" data-method="phone">휴대폰 결제</div>
            </div>
        </div>
        
        <button class="pay-button" onclick="requestPay()">
            안전하게 결제하기
        </button>
        
        <div class="security-info">
            🔒 모든 결제는 SSL 암호화로 보호됩니다<br>
            KG이니시스 안전결제 시스템을 사용합니다
        </div>
        
        <a href="<%= returnUrl %>" class="back-link">← 돌아가기</a>
    </div>
    
    <div class="loading" id="loading">
        <div class="loading-content">
            <div class="spinner"></div>
            <div>결제 처리 중입니다...</div>
        </div>
    </div>

    <script>
        let selectedMethod = 'card';
        
        // Payment method selection
        document.querySelectorAll('.method-btn').forEach(btn => {
            btn.addEventListener('click', function() {
                document.querySelectorAll('.method-btn').forEach(b => b.classList.remove('selected'));
                this.classList.add('selected');
                selectedMethod = this.dataset.method;
            });
        });
        
        // Set default selection
        document.querySelector('.method-btn[data-method="card"]').classList.add('selected');
        
        // Initialize IMP
        var IMP = window.IMP;
        IMP.init('<%= impKey %>');
        
        function requestPay() {
            const loading = document.getElementById('loading');
            loading.style.display = 'flex';
            
            IMP.request_pay({
                pg: 'html5_inicis',
                pay_method: selectedMethod,
                merchant_uid: '<%= merchantUid %>',
                name: '<%= sessionData.packageName || "코인 패키지" %>',
                amount: <%= sessionData.amount %>,
                buyer_email: '<%= sessionData.userEmail || "" %>',
                buyer_name: '<%= sessionData.userName || "고객" %>',
                buyer_tel: '',
                buyer_addr: '',
                buyer_postcode: ''
            }, function(rsp) {
                loading.style.display = 'none';
                
                // Send result to server for verification
                fetch('/payment/verify', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({
                        imp_uid: rsp.imp_uid,
                        merchant_uid: rsp.merchant_uid,
                        imp_success: rsp.success
                    })
                })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        alert(data.message);
                        window.location.href = data.redirectUrl;
                    } else {
                        alert(data.message);
                        if (data.redirectUrl) {
                            window.location.href = data.redirectUrl;
                        }
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    alert('결제 처리 중 오류가 발생했습니다.');
                    window.location.href = '<%= returnUrl %>/coins?payment=error';
                });
            });
        }
    </script>
</body>
</html>
